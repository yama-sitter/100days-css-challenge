<!-- index=55 -->
doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    link(rel="stylesheet" href="style.css")
    title 100days CSS Challenge
  body
    .ly_frame
      .bl_flower
        each i in [...Array(15).keys()]
          div.bl_flower_leaf(class=`bl_flower_leaf_${i}`)
        .bl_flower_dot
